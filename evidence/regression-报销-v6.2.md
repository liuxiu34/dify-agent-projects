# 报销预审 Agent · 云端回归记录（v6.2，2026-09-07）

- 方式：Dify 云端应用 API 直连（`/v1/chat-messages`，blocking），浏览器 UA
- 版本：报销 Agent v6.2（24 节点；会话状态机多轮补全 / 串单防护 / 发票非硬门槛 / 身份席别权限 / 超标只说明不索要文字）
- 用例：13 条 → **12 条通过**，1 条为已记录扩展边界（M3，同类多明细暂只解析首条，待支持）

| # | 场景 | 预期 | 结果 |
|---|---|---|---|
| D1 | 闲聊引导「你好，你是谁？」 | 自我介绍 + 8 步填写顺序 | ✅ |
| A1 | 合规住宿（成都 500×2 晚，有票） | L0 直通通过 | ✅ |
| A2 | 超标住宿（上海 900×2 晚，有票） | L1；说明行写明「900 vs 700」，不索要超标原因 | ✅ |
| A3 | 有明细未给发票 | 诊断照出 + 发票有效性存疑提示 | ✅ |
| C5 | 声明无发票（发票丢失） | 诊断照出 +「发票缺失·能否通过说明」+ 转特批路径；全文无"水单" | ✅ |
| E2 | 普通员工坐一等座 | 席别不合规 → 复核；不索要文字说明 | ✅ |
| M1 | 跨类别多明细（住宿+高铁，全合规） | 两条均解析 → 通过 | ✅ |
| M3 | 同类别两条住宿 | **已知边界**：当前只解析首条（500），900 超标条目未被判定，并提示发票金额不一致——待支持多条目 | ⚠️ 记录 |
| B1→B3 | 多轮补全：姓名→补明细→补全 | 证据不足→续补引导→放行 L1 超标诊断 | ✅ |
| D2 | 串单防护：完整单后同会话发新员工 | 从零收集新员工，不继承上一单 | ✅ |

## 里程碑修复记录
- v6.1：L1/L2 出口模板变量引用由单括号 `{#...#}` 修正为 Dify 标准双括号 `{{#...#}}`（用户结论行此前未渲染）
- v6.2：L1/L2 提示词加硬约束——禁止以任何形式索取文字性说明（超标原因/情况说明/书面解释等）；超标问题【需要您做的】只写"按超标审批流程处理"或列出缺失实物凭证
